spring:
  profiles:
    include: secret
    active: test

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/amp_test?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: z1227530!

  jpa:
    hibernate:
      ddl-auto: create-drop  # 테스트 후 자동으로 테이블 삭제
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  sql:
    init:
      mode: never

# JWT 테스트용 설정
jwt:
  secret: test-secret-key-for-testing-purposes-only-32-characters-long
  expiration: 86400000
  refresh-expiration: 604800000

# OAuth2 테스트용 설정 (필요시)
app:
  oauth2:
    redirect-uri: http://localhost:3000/oauth2/redirect
    onboarding-uri: http://localhost:3000/onboarding
    failure-redirect-uri: http://localhost:3000/login

logging:
  level:
    com.amp: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
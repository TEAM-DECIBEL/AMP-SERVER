spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: false

  cloud:
    aws:
      s3:
        bucket: ${S3_BUCKET_NAME}
      credentials:
        access-key: ${S3_ACCESS_KEY}
        secret-key: ${S3_SECRET_KEY}
      region:
        static: ${S3_REGION:ap-northeast-2}
      stack:
        auto: false

  security:
    oauth2:
      client:
        registration:
          google:
            redirect-uri: "https://api.ampnotice.kr/login/oauth2/code/{registrationId}"

jwt:
  cookie-secure: true
  cookie-domain: ""

app:
  oauth2:
    failure-redirect-uri: https://ampnotice.kr/login

  cors:
    allowed-origins: https://www.ampnotice.kr,https://ampnotice.kr,https://www.ampnotice-host.kr,https://ampnotice-host.kr,http://localhost:5173,http://localhost:5174
    allowed-methods: GET,POST,PUT,DELETE,PATCH,OPTIONS
    allowed-headers: "*"
    allow-credentials: true

springdoc:
  swagger-ui:
    enabled: true
  api-docs:
    enabled: true

server:
  port: 8080

swagger:
  server:
    prod:
      url: https://api.ampnotice.kr

logging:
  level:
    root: INFO
    com.amp: DEBUG
    com.amp.global.filter: INFO
    com.amp.global.config.FilterConfig: INFO
    org.springframework.security: DEBUG
    com.amp.global.security: DEBUG

fcm:
  key:
    path: /firebase-adminsdk.json
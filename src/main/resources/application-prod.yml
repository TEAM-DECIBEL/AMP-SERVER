spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: false

  security:
    oauth2:
      client:
        registration:
          google:
            redirect-uri: "https://www.ampnotice.kr/login/oauth2/code/google"

  cloud:
    aws:
      s3:
        bucket: ${S3_BUCKET_NAME}
      credentials:
        access-key: ${S3_ACCESS_KEY}
        secret-key: ${S3_SECRET_KEY}
      region:
        static: ${S3_REGION:ap-northeast-2}
      stack:
        auto: false

app:
  oauth2:
    redirect-uri: http://localhost:5173/auth/callback
    onboarding-uri: http://localhost:5173/onboarding
    failure-redirect-uri: https://www.ampnotice.kr/login

  cors:
    allowed-origins: >
      https://www.ampnotice.kr,
      https://ampnotice.kr,
      http://www.ampnotice.kr,
      http://ampnotice.kr
    allowed-methods: GET,POST,PUT,DELETE,PATCH,OPTIONS
    allowed-headers: "*"
    allow-credentials: true

# ✅ springdoc 쓰는 경우(있으면 적용됨). Swagger가 API를 www로 호출하도록 서버 URL 고정
springdoc:
  swagger-ui:
    enabled: true
  api-docs:
    enabled: true
  server-url: https://www.ampnotice.kr

server:
  port: 8080

logging:
  level:
    root: INFO
    com.amp: INFO
    org.springframework.security: INFO
